/*
 * app settings
 */
'use strict';

/**
 * appSettings - all relative to the project root
 */
const appSettings = {
    apihost: 'https://juronghuang.me:8000',
    publicDir: 'public/',
};

module.exports = appSettings;
